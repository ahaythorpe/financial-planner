name: Keepalive

on:
  schedule:
    - cron: '0 8 * * */3'  # Every 3 days at 8am UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: |
          curl -s -o /dev/null -w "%{http_code}" "${{ secrets.APP_URL }}"
